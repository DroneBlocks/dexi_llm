[
  {
    "type": "function",
    "function": {
      "name": "get_topics",
      "description": "List all available ROS 2 topics and their message types.",
      "parameters": {"type": "object", "properties": {}, "required": []}
    }
  },
  {
    "type": "function",
    "function": {
      "name": "subscribe_once",
      "description": "Subscribe to a topic and return the first message received. Useful for reading sensor data, detections, or drone state.",
      "parameters": {
        "type": "object",
        "properties": {
          "topic": {"type": "string", "description": "The topic name, e.g. '/fmu/out/battery_status'"},
          "msg_type": {"type": "string", "description": "Fully qualified message type, e.g. 'px4_msgs/msg/BatteryStatus'"}
        },
        "required": ["topic", "msg_type"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "call_service",
      "description": "Call a ROS 2 service and return the response. Use this for drone commands and LED control.",
      "parameters": {
        "type": "object",
        "properties": {
          "service": {"type": "string", "description": "Service name, e.g. '/dexi/execute_blockly_command'"},
          "service_type": {"type": "string", "description": "Fully qualified service type, e.g. 'dexi_interfaces/srv/ExecuteBlocklyCommand'"},
          "args": {"type": "object", "description": "Request fields as key-value pairs."}
        },
        "required": ["service", "service_type"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "publish_message",
      "description": "Publish a single message to a ROS 2 topic.",
      "parameters": {
        "type": "object",
        "properties": {
          "topic": {"type": "string", "description": "The topic name to publish to."},
          "msg_type": {"type": "string", "description": "Fully qualified message type."},
          "message": {"type": "object", "description": "Message fields as key-value pairs."}
        },
        "required": ["topic", "msg_type", "message"]
      }
    }
  }
]
